function like_neil(){
    contador = document.querySelector(".conteiner-principal .algunos-usuarios #like_neil");
    valor = parseInt(contador.innerHTML);
    valor = valor + 1;
    contador.innerHTML = valor;
}

function like_nichole(){
    contador = document.querySelector(".conteiner-principal .algunos-usuarios #like_nichole");
    valor = parseInt(contador.innerHTML);
    valor = valor + 1;
    contador.innerHTML = valor;
}

function like_jim(){
    contador = document.querySelector(".conteiner-principal .algunos-usuarios #like_jim");
    valor = parseInt(contador.innerHTML);
    valor = valor + 1;
    contador.innerHTML = valor;
}